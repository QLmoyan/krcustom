"use client";

/**
 * Service detail action panel — chat CTA lives on StoreInfoCard.
 * Kept as a non-chat info slot for optional future non-CTA content.
 */
export function ServiceActionPanel() {
  return null;
}
